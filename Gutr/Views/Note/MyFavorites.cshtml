@model IEnumerable<Gutr.Models.NoteListItem>

@{
    /**/

    ViewBag.Title = "MyFavorites";
}

<h2>My Favorites</h2>
@*<table class="table">*@
    @foreach (var item in Model)
    {
        @*<tr>*@
            @*<td>*@
                @*@value = Html.DisplayFor(modelItem => item.userEmail)
                    @substring = value.Substring(0, 5)*@
            @*</td>*@
            @*<td>
                    @Html.DisplayFor(modelItem => item.NoteId)
                </td>*@
            @*<td>*@
<div class="post-wrapper">
    <div><h3><a href="#">@Html.DisplayFor(modelItem => item.userEmail)</a> favorited:  </h3></div>
    <div class="post-message">
        @Html.DisplayFor(modelItem => item.Title)
        <a href="@Html.DisplayFor(modelItem => item.Content)" target="blank">@Html.DisplayFor(modelItem => item.Content)</a>
    </div><div>Author: @Html.DisplayFor(modelItem => item.Author)</div>
    
    @Html.ActionLink("user profile", "Index", "Profiles")@*, new { noteId = item.NoteId }, null)*@
    
                    @*<div class="ts">@Html.DisplayFor(modelItem => item.CreatedUtc)</div>*@
                    @Html.ActionLink("unFavorite this", "unFavorite", "Favorite", new { noteId = item.NoteId }, null)
                <div class="noteid">Post #@Html.DisplayFor(modelItem => item.NoteId)</div>
                @*</div>*@
            @*</td>
        </tr>*@
    </div>
    }






@*</table>*@